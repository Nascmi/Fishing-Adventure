import { fish } from './fish'
export const locations = [{id:'willow-pond',name:'Willow Pond',description:'A quiet, reed-fringed pond beneath the willows.',fishIds:fish.map(item=>item.id)}]
export const futureLocations = ['Pine River','Mountain Lake','Coastal Bay','Deep Sea']
